# X-Game AI 核心开发准则

作为本项目的 AI 编码助手，你在编写或修改代码前必须绝对遵守以下四大准则：

1. **项目说明书同步更新**：
   每完成一个新功能，必须自动修改 `docs/FEATURES.md` 和 `docs/CHANGELOG.md`，保证文档和代码始终保持同步。

2. **多语言与多主题 (I18n & Theming)**：
   - **禁止硬编码任何新增的 UI 文本**，所有文案必须抽离到 `i18n.service.ts` 及翻译字典里。
   - **禁止硬编码任何固定的颜色样式**（如 `bg-slate-900`, `text-white`），必须严格使用全局 CSS 变量（如 `var(--color-bg-main)`，或在 Tailwind 中映射的主题变量），确保 Light/Dark 主题可以无缝完美切换。

3. **高可扩展性设计**：
   保持前后端松耦合。未来新增任何游戏，必须在后端实现独立的 `GameEngine` 接口并接入 WebSocket Manager，前端必须新建独立的游戏路由模块（如 `features/games/new-game`），绝不可将单个游戏的独立逻辑深度侵入到公共组件中，避免牵一发而动全身。

4. **全端响应式适配**：
   编写的任何 UI 界面必须使用 TailwindCSS 的响应式前缀（`sm:`, `md:`, `lg:` 等）以完美适配手机、平板和桌面端。所有游戏面板、聊天框、弹窗和列表，不能在移动端出现横向滚动条等布局崩坏现象。
